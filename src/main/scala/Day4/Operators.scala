import scala.io.StdIn._

package Day4{

object Operators extends App {

// Create a method that takes in two integers and returns and integer
  def twoIntInput (numOne : Int, numTwo : Int ) : Int = {

// Within the body of the method specify what exactly you want to with the integers
    numOne + numTwo


  }
  // Within the println call the method (twoIntInput) and pass through the integers you want to add
        println(twoIntInput(10,9))

      }
}